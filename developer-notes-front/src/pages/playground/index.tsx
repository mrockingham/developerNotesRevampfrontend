import React, { useState, useCallback } from 'react';
import PlayGround from './PlayGround';

const index = () => {
  return (
    <div>
      <PlayGround />
    </div>
  );
};

export default index;
